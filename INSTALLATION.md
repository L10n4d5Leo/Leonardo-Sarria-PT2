# Leonardo-Sarria-PT2
# Configuracion
# Creació de la màquina virtual
- Abrir IsardVDI e iniciar sesión con tus credenciales.
- Seleccionar la opción de "Crear nueva máquina virtual".
- Buscar la maquina virtual que usaremos 
- despues entras,abresw la terminal para empezar a configurar para instalar
  ## Empezar con los comandos para instalar LAMP a UBUNTU
 - Primero abrimos la terminal y hacemos esto dentro...
-Actualitza el sistema con "sudo apt update && sudo apt upgrade -y"
- Despues Instala Apache con "sudo apt install apache2 -y"
- Ahora activamos e iniciamos el servicio 
